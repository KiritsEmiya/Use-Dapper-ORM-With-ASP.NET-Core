@using Acmilan.Models
@model IEnumerable<Movie>

<div class="container">
    <div class="row">
        <table class="table">
            <thead class="thead-dark">
                <tr>
                    <th scope="col">Id</th>
                    <th scope="col">CinemaId</th>
                    <th scope="col">Name</th>
                    <th scope="col">Starring</th>
                    <th scope="col">Release Date</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @Html.DisplayForModel()
            </tbody>
        </table>
        <a asp-action="Add" asp-route-cinemaId="@ViewBag.CinemaId"><button type="button" class="btn btn-primary">添加电影</button></a>
    </div>
</div>
